---
mode: 'agent'
description: 'Turn a purpose into a safe local .NET 10 prototype or a no-write integration plan.'
---

Follow the complete **build-custom-agent** workflow in
`.github/copilot-instructions.md`.

For each required two-choice gate, call #tool:vscode/askQuestions immediately
after its context and wait. Cancellation or no answer is not approval. Treat
any text supplied after the slash command as the request.

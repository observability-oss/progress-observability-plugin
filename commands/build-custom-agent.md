---
description: Turn a purpose into either a safe local .NET 10 agent prototype or a no-write integration plan.
argument-hint: "optional purpose; optional Try-and-refine: on|off"
---

Use the `build-custom-agent` skill for this request.

Purpose and preferences: $ARGUMENTS

For every two-choice gate, call the host's native question tool immediately
after its required context and wait. Never treat cancellation or no answer as
approval.

# Reference names only. Export them in the shell or use dotnet user-secrets.
# Do not create .env here: the app does not load it and validation rejects it.
AzureOpenAI__Endpoint=https://YOUR-RESOURCE.openai.azure.com/
AzureOpenAI__Deployment=gpt-4.1
AzureOpenAI__ApiKey=YOUR-AZURE-OPENAI-KEY
Progress__Observability__ApiKey=YOUR-PROGRESS-INTEGRATION-KEY
